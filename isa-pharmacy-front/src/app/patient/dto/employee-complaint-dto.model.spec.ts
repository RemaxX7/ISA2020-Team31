import { EmployeeComplaintDTO } from './employee-complaint-dto.model';

describe('EmployeeComplaintDTO', () => {
  it('should create an instance', () => {
    expect(new EmployeeComplaintDTO()).toBeTruthy();
  });
});
